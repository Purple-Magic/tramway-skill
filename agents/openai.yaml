interface:
  display_name: "Rails Project Manager"
  short_description: "Safe Rails workflows for build, debug, and release"
  default_prompt: "Use this skill for explicit Rails work. First make one minimal Rails-app check in the current directory, then continue with the relevant workflow."
