# Repository Instructions

Follow all instructions in `AGENTS.md`. At the end of every task that changes this repository, update `skills/tramway-skill/VERSION` and sync with:

```bash
rm -rf ~/.claude/skills/tramway-skill
cp -R ./skills/tramway-skill ~/.claude/skills/tramway-skill
```
